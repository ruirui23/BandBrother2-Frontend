export const HIT_X = 160 // 判定線の X 座標(px)
export const HIT_Y =
  typeof window !== 'undefined' ? window.innerHeight - 120 : 600 // 判定線の Y 座標(px)
export const NOTE_SPEED = 300 // ノーツの移動速度(px/sec)
export const WINDOW_SEC = 4 // 何秒先まで描画するか
